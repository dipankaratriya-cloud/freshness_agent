# Copy this file to .env and fill in real values. Never commit .env itself.
GROQ_API_KEY=
GITHUB_TOKEN=

# GEMINI_API_KEY is no longer used by this pipeline (Tier 3 was migrated to
# Groq after the Gemini credential was found to be invalid) — kept here only
# if you plan to restore/re-test Gemini later.
GEMINI_API_KEY=
